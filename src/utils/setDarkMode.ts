import AsyncStorage from '@react-native-async-storage/async-storage';

export const SET_DARK_MODE = '@dark_mode';

// if first launch
// set mode to light

// if not first launch, get theme mode

// a function should be able to read theme mode
